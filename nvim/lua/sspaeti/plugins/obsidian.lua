
return {
--  "epwalsh/obsidian.nvim",
--  event = "VeryLazy",  --using neovim with the Obsidian vault
--  config = function()
--     require("obsidian").setup({
--       dir = "$HOME/Simon/Sync/SecondBrain",
--       completion = {
--         nvim_cmp = true, -- if using nvim-cmp, otherwise set to false
--       }
--     })
--   end
}
