# See https://wiki.hyprland.org/Configuring/Monitors/

# List current monitors and resolutions possible: hyprctl monitors
# Format: monitor = [port], resolution, position, scale
# You must relaunch Hyprland after changing any envs (use Super+Esc, then Relaunch)

# Optimized for retina-class 2x displays, like 13" 2.8K, 27" 5K, 32" 6K.
# env = GDK_SCALE,2
# monitor=,preferred,auto,auto

# Good compromise for 27" or 32" 4K monitors (but fractional!)
# env = GDK_SCALE,1.75
# monitor=,preferred,auto,1.666667

# Straight 1x setup for low-resolution displays like 1080p or 1440p
# env = GDK_SCALE,1
# monitor=,preferred,auto,1

# Example for Framework 13 w/ 6K XDR Apple display
# monitor = DP-5, 6016x3384@60, auto, 2
# monitor = eDP-1, 2880x1920@120, auto, 2


# ---------------------------------------------------------------------------------------

# ============================================================================
# SINGLE SOURCE OF TRUTH
# Define each monitor I own once. Change ports here if they swap or break.
# Add new monitors below by following the same pattern.
# ============================================================================

# Match Dells by description, not port. The port (DP-1, DP-2, HDMI-*) doesn't
# matter anymore — Hyprland resolves "desc:<substring>" to whatever port the
# monitor is currently plugged into. Confirm description with: hyprctl monitors

# --- HOME Dell (Dell S2722QC, 27" 4K UHD) ---
$dellHomePort  = desc:Dell Inc. DELL S2722QC
$dellHomeRes   = 3840x2160@60
$dellHomeScale = 1.5

# --- WORK Dell (Dell S2725QC, 4K UHD) ---
# TODO: verify exact description at office (hyprctl monitors -j)
# $dellWorkPort  = desc:Dell Inc. DELL S2722QC 89CQLD3
$dellWorkPort  = desc:Dell Inc. DELL S2725QC DTFF464
$dellWorkRes   = 3840x2160@60
$dellWorkScale = 1.5

# --- TUXEDO laptop internal ---
$laptop      = eDP-1
$laptopRes   = 2880x1800@120
$laptopScale = 1.8

# --- Layout positions ---
$posExtTopLeft    = 0x0           # external at origin
$posHomeLaptop    = 480x1440      # laptop BELOW external (home stack)
$posOfficeLaptop  = 2560x220      # laptop RIGHT of external (office)
$posStandaloneLaptop = 0x0        # laptop standalone
$posHdmiLaptop    = 1920x0        # laptop right of HDMI (extended)

# --- Workspace assignment is dynamic ---
# At boot, workspaces 1-5 default to $dellHomePort (see bottom of file).
# When you press SUPER+ALT+7 (home) or SUPER+ALT+8 (office), the keybind
# reassigns workspaces 1-5 to whichever Dell port is in use, so you only
# ever need to update $dellHomePort or $dellWorkPort above.

# --- Composable hyprctl --batch fragments (joined with ' ; ') ---
# Monitor layouts
$layoutHome        = keyword monitor $dellHomePort,$dellHomeRes,$posExtTopLeft,$dellHomeScale ; keyword monitor $laptop,$laptopRes,$posHomeLaptop,$laptopScale
$layoutHomeExtOnly = keyword monitor $dellHomePort,$dellHomeRes,$posExtTopLeft,$dellHomeScale ; keyword monitor $laptop,disable
$layoutOffice        = keyword monitor $dellWorkPort,$dellWorkRes,$posExtTopLeft,$dellWorkScale ; keyword monitor $laptop,$laptopRes,$posOfficeLaptop,$laptopScale
$layoutOfficeExtOnly = keyword monitor $dellWorkPort,$dellWorkRes,$posExtTopLeft,$dellWorkScale ; keyword monitor $laptop,disable
$layoutLaptopOnly    = keyword monitor $dellHomePort,disable ; keyword monitor $dellWorkPort,disable ; keyword monitor $laptop,$laptopRes,$posStandaloneLaptop,$laptopScale

# Workspace assignments (1-5 to external, 6-10 stay on laptop)
$wsToHomeExt  = keyword workspace 1,monitor:$dellHomePort ; keyword workspace 2,monitor:$dellHomePort ; keyword workspace 3,monitor:$dellHomePort ; keyword workspace 4,monitor:$dellHomePort ; keyword workspace 5,monitor:$dellHomePort
$wsToWorkExt  = keyword workspace 1,monitor:$dellWorkPort ; keyword workspace 2,monitor:$dellWorkPort ; keyword workspace 3,monitor:$dellWorkPort ; keyword workspace 4,monitor:$dellWorkPort ; keyword workspace 5,monitor:$dellWorkPort
$wsToLaptop   = keyword workspace 1,monitor:$laptop ; keyword workspace 2,monitor:$laptop ; keyword workspace 3,monitor:$laptop ; keyword workspace 4,monitor:$laptop ; keyword workspace 5,monitor:$laptop
$wsLaptopHi   = keyword workspace 6,monitor:$laptop ; keyword workspace 7,monitor:$laptop ; keyword workspace 8,monitor:$laptop ; keyword workspace 9,monitor:$laptop ; keyword workspace 10,monitor:$laptop


# Monitor and Screen resolution --TUXEDO

# Desc for 4: For example, if you have a 27" or 32" 4K, you can use fractional scaling (env = GDK_SCALE,1.75 and monitor=,preferred,auto,1.66666)
# Fallback: Use single default monitor (see all monitors with: hyprctl monitors)
# for 4k (but works also for leneovo native). Optimal for lenovo would be scale=1
# monitor=,preferred,auto,1.666667
env = GDK_SCALE,1.75 #for 4k

# ===== STATIC MONITOR DEFINITIONS =====
# These apply when Hyprland starts

# # External Dell 4K monitor (works for both home and office)
# monitor = $dellHomePort, $dellHomeRes, $posExtTopLeft, $dellHomeScale

# # TUXEDO laptop internal screen (default) - append mirror for demo-presentations
# monitor = $laptop, $laptopRes, $posHomeLaptop, $laptopScale #, mirror, $dellHomePort

# MIRROR for PRESENTATIONs -> Comment out above and use below -> Or just append `mirror,DP-1` or `mirror,HDMI-A-1` to above. Check with `hyprctl monitors`
##-1 DisplayPort:
#monitor = $laptop, $laptopRes, 0x0, $laptopScale, mirror, $dellHomePort
##-2 HDMI:
#monitor = $laptop, $laptopRes, 0x0, $laptopScale, mirror, $hdmi
#monitor = eDP-1, 2880x1800@120, 0x0, 1.8, mirror, HDMI-1

# Fallback for any other monitors
monitor = , preferred, auto, auto


## SUPER+ALT+0 for autosetup for presentation
## Manual Setup: Use `hyprmon` to setup screen



# ===== DYNAMIC LAYOUT SWITCHING =====
# Only the POSITION changes between home and office

# SUPER ALT 7: HOME setup (external above, laptop below) -- Toggle to HOME setup
bindd = SUPER ALT, 7, Home Setup,        exec, hyprctl --batch "$layoutHome ; $wsToHomeExt ; $wsLaptopHi" && notify-send "Monitor Setup" "HOME: External above laptop"
bindd = SUPER ALT, 1, Home Ext Only,     exec, hyprctl --batch "$layoutHomeExtOnly ; $wsToHomeExt" && notify-send "Monitor Setup" "HOME: External only"

# SUPER ALT 8: OFFICE setup (external left, laptop right) -- Toggle to OFFICE setup
bindd = SUPER ALT, 8, Office Setup,      exec, hyprctl --batch "$layoutOffice ; $wsToWorkExt ; $wsLaptopHi" && notify-send "Monitor Setup" "OFFICE: Laptop right of external"
bindd = SUPER ALT, 2, Office Ext Only,   exec, hyprctl --batch "$layoutOfficeExtOnly ; $wsToWorkExt" && notify-send "Monitor Setup" "OFFICE: External only"

# SUPER ALT 9: Laptop only (disable both externals)
bindd = SUPER ALT, 9, Laptop Only,       exec, hyprctl --batch "$layoutLaptopOnly ; $wsToLaptop" && notify-send "Monitor Setup" "Laptop only mode"

# SUPER ALT 0: Auto setup (reset to defaults)
bindd = SUPER ALT, 0, Auto Setup, exec, hyprctl keyword monitor ",preferred,auto,auto" && notify-send "Monitor Setup" "AUTO setup"

# ===== HDMI SHORTCUTS =====
# TODO: Check when connected it shows up as HDMI-A-1 or HDMI-1
# SUPER ALT 4: Mirror internal laptop to HDMI (for presentations)
bindd = SUPER ALT, 4, HDMI Mirror, exec, hyprctl keyword monitor "$HDMI-A-1,preferred,auto,auto" && hyprctl keyword monitor "$laptop,$laptopRes,0x0,$laptopScale,mirror,$hdmi" && notify-send "Monitor Setup" "HDMI: Mirrored display"

# SUPER ALT 5: Extended display with HDMI (laptop + HDMI side by side)
bindd = SUPER ALT, 5, HDMI Extended, exec, hyprctl keyword monitor "$HDMI-A-1,preferred,0x0,auto" && hyprctl keyword monitor "$laptop,$laptopRes,$posHdmiLaptop,$laptopScale" && notify-send "Monitor Setup" "HDMI: Extended display"


# Cycle monitor scaling with SUPER + CTRL + / (slash)
bindd = SUPER CTRL, slash, Cycle monitor scaling, exec, omarchy-hyprland-monitor-scaling-cycle
bindd = SUPER CTRL ALT, slash, Cycle monitor scaling backwards, exec, omarchy-hyprland-monitor-scaling-cycle --reverse

## Scaling on dell home setup - Scaling comparison:
## - **1.0 (Native)**: 3840×2160 - Too small, text would be unreadable
## - **1.5 (seem best)**: 2560×1440 effective - Perfect balance ✅
## - **2.0**: 1920×1080 effective - Too large, wastes the 4K resolution

# ===== WORKSPACE ASSIGNMENT =====
# Boot defaults: workspaces 1-5 on $dellHomePort. SUPER+ALT+7/8 reassign at runtime.
workspace = 1, monitor:$dellHomePort
workspace = 2, monitor:$dellHomePort
workspace = 3, monitor:$dellHomePort
workspace = 4, monitor:$dellHomePort
workspace = 5, monitor:$dellHomePort

# Workspaces 6-10 on laptop screen (eDP-1)
workspace = 6, monitor:$laptop
workspace = 7, monitor:$laptop
workspace = 8, monitor:$laptop
workspace = 9, monitor:$laptop
workspace = 10, monitor:$laptop

# Fallback: when no external monitor, all workspaces on laptop
workspace = 1, monitor:$laptop, default:true
workspace = 2, monitor:$laptop, default:true
workspace = 3, monitor:$laptop, default:true
workspace = 4, monitor:$laptop, default:true
workspace = 5, monitor:$laptop, default:true


# Cycle monitor scaling
bindd = SUPER ALT CTRL, Slash, Cycle monitor scaling, exec, omarchy-hyprland-monitor-scaling-cycle

## HOME SETUP: External monitor (top) - Dell S2722QC at native 4K resolution
## Laptop screen on the bottom
## monitor=DP-1,3840x2160@60,0x0,1.5
## monitor=eDP-1,1920x1200@60,0x1440,1.5

## OFFICE SETUP: Dell display to the left of laptop
## Uncomment these lines for office setup:
##monitor=DP-1,3840x2160@60,0x0,1.5
##monitor=eDP-1,1920x1200@60,2560x0,1.25

##set default if no other send:
# monitor=,preferred,auto,auto
