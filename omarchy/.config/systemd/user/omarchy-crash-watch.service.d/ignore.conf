# Process names (extended regex on the executable basename) that
# omarchy-crash-watch should never announce. enteauth segfaults on every
# normal quit (SUPER+Q), so its "crash" is just the app closing.
[Service]
Environment=OMARCHY_CRASH_IGNORE=^enteauth$
